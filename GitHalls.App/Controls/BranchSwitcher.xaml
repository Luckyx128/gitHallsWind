<UserControl
    x:Class="GitHalls.App.Controls.BranchSwitcher"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:GitHalls.App.Controls">

    <Grid Width="300">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <controls:BranchPicker x:Name="Picker" Grid.Row="0" MinHeight="220" MaxHeight="340" />

        <StackPanel Grid.Row="1" Spacing="6" Padding="8"
                    BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,1,0,0">
            <TextBlock Text="New branch" FontSize="12" FontWeight="SemiBold"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <Grid ColumnSpacing="6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="NewBranchTextBox" Grid.Column="0"
                         PlaceholderText="feature/login"
                         TextChanged="NewBranchTextBox_TextChanged"
                         KeyDown="NewBranchTextBox_KeyDown" />
                <Button x:Name="CreateBranchButton" Grid.Column="1" Content="Create"
                        IsEnabled="False" Click="CreateBranchButton_Click" />
            </Grid>
            <TextBlock x:Name="CreateHintText" FontSize="12" TextWrapping="Wrap"
                       Visibility="Collapsed"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>
    </Grid>
</UserControl>
